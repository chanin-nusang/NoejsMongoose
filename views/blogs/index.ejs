<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>REALCHANIN</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
    <%- include('../layout') %> 
    <div class="py-2">
    <a href="/blogs/add" class="btn btn-primary">เขียนบทความ</a>
    </div>
    <hr>
    <h2><%= data %></h2>
    <p>by REALCHANIN</p>
    <p>
        <% Object.values(blogs).forEach(function(blog){ %>
            <h5><b>ชื่อบทความ : </b><%= blog.title %></h5>
            <p><b>ผู้แต่ง : </b><%= blog.author %></p>
            <p><b>หมวดหมู่ : </b><%= blog.category %></p>
            <a href="/blogs/edit/<%= blog._id %>">แก้ไข</a>
            <a href="/blogs/delete/<%= blog._id %>" 
                onclick="return confirm('ต้องการลบบทความ <%= blog.title %> หรือไม่?')">ลบ</a>
            <hr>
        <% }) %>
    </p>
</body>
</html>